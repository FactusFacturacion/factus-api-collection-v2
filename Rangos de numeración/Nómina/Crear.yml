info:
  name: Crear
  type: http
  seq: 1

http:
  method: POST
  url: "{{url_api}}/v2/numbering-ranges/payrolls"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
