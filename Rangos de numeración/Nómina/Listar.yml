info:
  name: Listar
  type: http
  seq: 4

http:
  method: GET
  url: "{{url_api}}/v2/numbering-ranges/payrolls"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
