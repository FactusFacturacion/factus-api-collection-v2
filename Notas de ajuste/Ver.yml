info:
  name: Ver
  type: http
  seq: 2

http:
  method: GET
  url: "{{url_api}}/v2/adjustment-notes/:number"
  params:
    - name: number
      value: NDA65
      type: path
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
