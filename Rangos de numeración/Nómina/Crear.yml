info:
  name: Crear
  type: http
  seq: 1

http:
  method: POST
  url: "{{url_api}}/v2/numbering-ranges/payrolls"
  body:
    type: json
    data: |-
      {
        "document": "26",
        "prefix": "NE",
        "current": "1"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
