info:
  name: Ver
  type: http
  seq: 3

http:
  method: GET
  url: "{{url_api}}/v2/payrolls/reference/:reference_code"
  headers:
    - name: Accept
      value: application/json
  params:
    - name: reference_code
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  **Documentación:**

  [https://developers.factus.com.co/nomina/ver](https://developers.factus.com.co/nomina/ver)
