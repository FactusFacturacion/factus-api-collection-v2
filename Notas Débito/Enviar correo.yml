info:
  name: Enviar correo
  type: http
  seq: 9

http:
  method: POST
  url: "{{url_api}}/v2/debit-notes/:number/send-email"
  params:
    - name: number
      value: ND86
      type: path
  body:
    type: json
    data: |-
      {
          "email": "alanturing@enigmasas.com.com",
          "pdf_base_64_encoded": "sdfajdfka...."
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
