info:
  name: Descargar Pdf
  type: http
  seq: 6

http:
  method: GET
  url: "{{url_api}}/v2/payrolls/:number/download-pdf"
  headers:
    - name: Accept
      value: application/json
  params:
    - name: number
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
