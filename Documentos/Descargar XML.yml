info:
  name: Descargar XML
  type: http
  seq: 1

http:
  method: GET
  url: "{{url_api}}/v2/documents/:trackId/download-xml"
  params:
    - name: trackId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
