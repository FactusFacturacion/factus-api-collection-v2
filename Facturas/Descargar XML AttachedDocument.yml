info:
  name: Descargar XML AttachedDocument
  type: http
  seq: 8

http:
  method: GET
  url: "{{url_api}}/v2/bills/:number/download-attached-document-xml"
  headers:
    - name: Accept
      value: application/json
  params:
    - name: number
      value: SETP990001103
      type: path
  body:
    type: text
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
